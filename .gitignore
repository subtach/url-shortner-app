# Dependencies
node_modules/

# Next.js build output
.next/
out/

# Environment variables (never commit these)
.env
.env.local
.env*.local

# SQLite database files (local only, persisted in Turso for production)
links.db
links.db-shm
links.db-wal

# Misc
.DS_Store
*.pem
npm-debug.log*